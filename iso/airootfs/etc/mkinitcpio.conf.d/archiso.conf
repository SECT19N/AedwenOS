# plymouth: branded boot splash on the live medium (must sit right after udev,
# before archiso). Needs "splash" on the kernel cmdline -- see syslinux.cfg and
# efiboot/loader/entries/aedwen.conf.
HOOKS=(base udev plymouth modconf archiso block filesystems keyboard)
COMPRESSION="zstd"
