# AedwenOS: use the branded theme instead of upstream's default.
# `plymouth-set-default-theme` would just rewrite this + rebuild the initramfs;
# shipping it directly keeps the choice reproducible.
[Daemon]
Theme=aedwen
ShowDelay=0
DeviceTimeout=8
